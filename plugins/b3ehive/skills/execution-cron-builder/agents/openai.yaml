interface:
  display_name: "Execution Cron"
  short_description: "Build portable tmux Codex execution crons"
  default_prompt: "Use $execution-cron-builder to build a repository-agnostic blueprint execution cron with repository-selected bounded or persistent task-local tmux Codex /goal workers, exact concurrency and replacement accounting, and no app-server or exec fallback."
