interface:
  display_name: "Execution Cron"
  short_description: "Build portable tmux Codex execution crons"
  default_prompt: "Use $execution-cron-builder to build a repository-agnostic blueprint execution cron with one independent task-local tmux Codex TUI process and authenticated /goal per claim, with no app-server or exec fallback."
