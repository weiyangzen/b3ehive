interface:
  display_name: "Execution Cron"
  short_description: "Build portable execution crons for Cursor workers"
  default_prompt: "Use $execution-cron-builder to build a repository-agnostic blueprint execution cron with isolated Cursor workers, exact concurrency accounting, and no hidden child capacity."
